general:
  branches:
    ignore:
      - /dev.*/

machine:
  environment:
    TESTOPTS: -v
    TINYTDS_UNIT_HOST: localhost
  services:
    - docker

dependencies:
  post:
    - docker info

database:
  post:
    - ./test/bin/setup.sh

test:
  override:
    - bundle exec rake
